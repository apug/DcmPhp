include:
  - ${DCM_CONFIG_DIR}/DcmPhp/Php85/volumes.yml

services:
  php85:
    build:
      context: ../../lib
      dockerfile: Dockerfile
      args:
        PHP_VERSION: "8.5"
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    image: dcm-php:8.5
    restart: unless-stopped
    volumes:
      - ${DCM_VOLUMES_DIR}/DcmPhp/apps:/var/www/html:z
    networks:
      - web
      - db
    depends_on:
      caddy:
        condition: service_started
